# 原因码分类 v0

生成时间：2026-04-21。
适用范围：EvalShield v1。

## 目的

这份文档统一 reason code 的命名、归属、默认严重度与使用边界。

它不替代 `判定策略矩阵-v0.md`。
它的作用是防止 reason code 在实现期四处漂移。

## 结构

每个 reason code 至少定义：

- code
- family
- 含义
- 默认严重度
- 是否可作为 primary reason

## v1 初始 reason codes

| code | family | 含义 | 默认严重度 | 可否 primary |
|---|---|---|---|---|
| `leakage_hidden_answer` | leakage | run 接触了不该可见的 hidden answer | high | yes |
| `leakage_fixture_exposure` | leakage | run 接触了 fixture 泄露内容 | medium | yes |
| `leakage_hidden_metadata` | leakage | run 使用了不该暴露的 benchmark metadata | medium | yes |
| `exploit_harness_metadata` | exploit | run 利用 harness metadata 走捷径 | high | yes |
| `exploit_scaffold_shortcut` | exploit | run 利用 scaffold shortcut 绕过预期评测 | high | yes |
| `exploit_benchmark_structure` | exploit | run 依赖 benchmark 结构而非真实任务能力 | medium | yes |
| `replay_unavailable` | replay | replay 因前提不足无法执行 | low | yes |
| `replay_structural_mismatch` | replay | structural replay 发现结构级偏差 | medium | yes |
| `replay_behavioral_divergence` | replay | behavioral replay 出现实质行为差异 | medium | yes |
| `detector_error` | system | detector 自身执行失败 | low | conditional |
| `conflict_requires_review` | system | detector outputs 出现冲突，需要人工复核 | low | yes |

## 命名规则

- 全部使用 snake_case
- 前缀反映 family：
  - `leakage_`
  - `exploit_`
  - `replay_`
  - `detector_`
  - `conflict_`
- 不允许同义不同名

## Primary Reason 使用规则

- `detector_error` 只有在它确实影响 trust interpretation 时才能进入 primary reasons
- `replay_unavailable` 可以成为 primary reason，但通常不应压过高危 exploit / leakage
- family 内若已有更高严重度 code，低严重度 code 不应抢 primary slot

## 与 Verdict Policy 的关系

- `reason-code taxonomy` 负责 code 的词汇与语义
- `verdict-policy matrix` 负责 finding 组合如何映射到 verdict

这两者不能互相替代。

## 新增 reason code 的治理流程

新增 reason code 时，顺序必须固定：

1. 先更新 `原因码分类-v0.md`
2. 如果 verdict 行为受影响，再更新 `判定策略矩阵-v0.md`
3. 最后才允许在实现中落该 code

禁止在实现中先发明新 code，再回头补文档。

## 明确不做

- v1 不做层级化 ontology
- v1 不做数百个 reason code
- v1 不做跨 benchmark family 的泛化 taxonomy
